[project]
name = "paper-searcher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "arxiv>=2.4.0",
    "arxivql>=1.3.3",
    "streamlit>=1.52.2",
]

[dependency-groups]
dev = [
    "ruff>=0.14.11",
]
